#include <iostream>
#include <fstream>
#include <cstdlib>
#include <string>
#include <filesystem>
#include <vector>
using namespace std;

int main () {
    vector<int> num;
    num.push_back(20);
    num.push_back(45);

    cout << num[0] << " " << num[1] << endl;
    cout << num.back() << endl;
    for()
   
    return 0;
}